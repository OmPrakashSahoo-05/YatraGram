{
  "port": "env(PORT)",
  "settings": {
    "loggerFormat": "foal",
    "session": {
      "store": "@foal/typeorm",
      "csrf": {
        "enabled": true
      }
    },
    "disk": {
      "local": {
        "directory": "assets"
      }
    }
  },
  "database": {
    "type": "sqlite",
    "database": "./db.sqlite3"
  }
}
